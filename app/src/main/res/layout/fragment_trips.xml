<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".src.main.trips.TripsFragment"
    android:background="@android:color/white">
    <TextView
        android:id="@+id/trips_tv_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/trips_title"
        android:textSize="34sp"
        android:layout_marginLeft="25dp"
        android:layout_marginTop="5dp"
        android:layout_marginBottom="30dp"
        android:textColor="@color/mainTextColor"/>
    <com.google.android.material.tabs.TabLayout
        android:id="@+id/trips_tab"
        android:layout_width="wrap_content"
        android:layout_height="40dp"
        android:layout_marginHorizontal="20dp"
        android:layout_marginBottom="15dp"
        android:layout_below="@id/trips_tv_title"
        app:tabPaddingStart="10dp"
        app:tabPaddingEnd="10dp"
        app:tabIndicatorColor="@color/tripsIndicator"
        app:tabSelectedTextColor="@color/mainTextColor"
        app:tabTextColor="@color/tripsTabTextColor"/>

    <androidx.viewpager.widget.ViewPager
        android:id="@+id/trips_pager"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/trips_tab"/>

    <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentRight="true"
        android:layout_alignParentBottom="true"
        android:layout_margin="30dp"
        android:src="@drawable/ic_add"
        android:tint="@android:color/white"
        app:maxImageSize="18dp"
        android:backgroundTint="@color/tripsFabBackground"/>
</RelativeLayout>